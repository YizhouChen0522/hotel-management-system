package com.johnny.hotel.repair;import com.johnny.hotel.inspection.RoomInspection;import com.johnny.hotel.pagination.PageResult;public interface RepairOrderService{RepairOrder createForInspection(RoomInspection inspection,String description,Long actor);RepairOrder get(Long id);PageResult<RepairOrder> list(Long roomId,Integer status,Long inspectionId,Integer page,Integer pageSize);RepairOrder complete(Long id,RepairRequests.Complete request,boolean force);RepairOrder cancel(Long id,String note);}

package com.johnny.hotel.extras;import com.johnny.hotel.pagination.PageResult;import java.util.List;public interface GuestExtrasService{ChargeCatalog createCatalog(ExtrasRequests.Catalog request);ChargeCatalog updateCatalog(Long id,ExtrasRequests.Catalog request);ChargeCatalog setCatalogStatus(Long id,boolean active);PageResult<ChargeCatalog> catalogs(Integer type,Integer status,String query,Integer page,Integer pageSize);GuestServiceOrder createService(Long stayId,ExtrasRequests.ServiceOrder request);GuestServiceOrder claimService(Long id);GuestServiceOrder completeService(Long id,String note,boolean force);GuestServiceOrder cancelService(Long id,String note);PageResult<GuestServiceOrder> services(Long stayId,Integer status,Integer page,Integer pageSize);GuestPurchase createPurchase(Long stayId,ExtrasRequests.Purchase request);GuestPurchase confirmPurchase(Long id);GuestPurchase cancelPurchase(Long id);PageResult<GuestPurchase> purchases(Long stayId,Integer status,Integer page,Integer pageSize);List<GuestPurchaseItem> purchaseItems(Long id);}
